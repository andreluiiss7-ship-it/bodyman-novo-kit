---
import { getImage } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import CheckoutModal from "../components/CheckoutModal.astro";
import { KITS, DEFAULT_KIT_INDEX, discountPercent } from "../data/kits";
import { SITE } from "../config/site.config";
import heroImage from "../assets/products/hero.webp";
import vibrationImage from "../assets/products/vibration.webp";
import blunnImage from "../assets/products/blunn.webp";
import feroImage from "../assets/products/fero.webp";
import vibrationThumb from "../assets/products/vibration-thumb.webp";
import blunnThumb from "../assets/products/blunn-thumb.webp";
import feroThumb from "../assets/products/fero-thumb.webp";
import kit6Image from "../assets/products/kit-6-frascos.webp";
import cliente1 from "../assets/reviews/cliente-1.webp";
import cliente2 from "../assets/reviews/cliente-2.webp";
import cliente3 from "../assets/reviews/cliente-3.webp";
import cliente4 from "../assets/reviews/cliente-4.webp";

const title = SITE.productName;
const description = "Kit com 3 body splash masculinos de 200ml: Vibration (cítrico), Blunn (amadeirado, inspirado no Bleu de Chanel) e Infalível Fero (aquático). Uma fragrância pra cada momento do dia, com frete grátis e pagamento no Pix.";
const ogImage = await getImage({ src: heroImage, width: 1200, height: 1200 });

const initialKit = KITS[DEFAULT_KIT_INDEX];
const initialDiscount = discountPercent(initialKit);
const kitThumbs = [vibrationThumb, blunnThumb, feroThumb];

const brl = (v: number) => v.toFixed(2).replace(".", ",");

const productSchema = {
  "@context": "https://schema.org",
  "@type": "Product",
  name: title,
  description,
  brand: { "@type": "Brand", name: SITE.storeName },
  image: ogImage.src,
  offers: {
    "@type": "Offer",
    priceCurrency: "BRL",
    price: initialKit.price.toFixed(2),
    availability: "https://schema.org/InStock",
  },
  aggregateRating: {
    "@type": "AggregateRating",
    ratingValue: "4.8",
    reviewCount: "312",
  },
};
---
<Layout title={`${title} | ${SITE.storeName}`} description={description} ogImage={ogImage.src}>

  <!-- Ícones (symbols) reutilizáveis -->
  <svg width="0" height="0" style="position:absolute" aria-hidden="true">
    <symbol id="i-lock" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></symbol>
    <symbol id="i-truck" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 4h13v11H1z"/><path d="M14 8h4l4 4v3h-8"/><circle cx="6" cy="18" r="2"/><circle cx="18" cy="18" r="2"/></symbol>
    <symbol id="i-shield" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/></symbol>
    <symbol id="i-star" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01z"/></symbol>
    <symbol id="i-check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></symbol>
    <symbol id="i-zap" viewBox="0 0 24 24" fill="currentColor"><path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"/></symbol>
    <symbol id="i-arrows" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 7 4 12 9 17"/><polyline points="15 7 20 12 15 17"/></symbol>
    <symbol id="i-spark" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l1.6 5.4L19 9l-5.4 1.6L12 16l-1.6-5.4L5 9l5.4-1.6z"/></symbol>
    <symbol id="i-tooth" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3c-2.5 0-4.5 1.6-4.5 4.2 0 1 .2 1.7.5 2.7.3 1 .6 2.2.7 4 .1 1.3.5 2.1 1.1 2.1.7 0 .9-.9 1.1-2.3.2-1.5.5-2.3 1.1-2.3.6 0 .9.8 1.1 2.3.2 1.4.4 2.3 1.1 2.3.6 0 1-.8 1.1-2.1.1-1.8.4-3 .7-4 .3-1 .5-1.7.5-2.7C16.5 4.6 14.5 3 12 3Z"/></symbol>
    <symbol id="i-leaf" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M11 20A7 7 0 0 1 4 13H2s1-6 8-6 12 3 12 3-2 12-11 10z"/><path d="M2 22l7-7"/></symbol>
    <symbol id="i-coffee" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M17 8h1a4 4 0 1 1 0 8h-1M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4z"/><line x1="6" y1="2" x2="6" y2="4"/><line x1="10" y1="2" x2="10" y2="4"/><line x1="14" y1="2" x2="14" y2="4"/></symbol>
    <symbol id="i-chat" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></symbol>
    <symbol id="i-pix" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l4 4-4 4-4-4z"/><path d="M2 12l4-4 4 4-4 4z"/><path d="M22 12l-4-4-4 4 4 4z"/><path d="M12 14l4 4-4 4-4-4z"/></symbol>
    <symbol id="i-vol" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 5L6 9H2v6h4l5 4V5z"/><path d="M15.5 8.5a5 5 0 0 1 0 7"/></symbol>
    <symbol id="i-plus" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></symbol>
    <symbol id="i-vf" viewBox="0 0 40 40"><path fill="var(--wine)" d="M19.998 3.094L14.638 0l-2.972 5.15H5.432v6.354L0 14.64 3.094 20 0 25.359l5.432 3.137v5.905h5.975L14.638 40l5.36-3.094L25.358 40l3.232-5.6h6.162v-6.01L40 25.359 36.905 20 40 14.641l-5.248-3.03v-6.46h-6.419L25.358 0l-5.36 3.094z"/><path fill="#fff" d="M18 27.36l-6.6-6.6 2.83-2.82L18 21.72l7.77-7.77 2.83 2.83z"/></symbol>
  </svg>

  <!-- ================ TOPBAR ROTATIVA ================ -->
  <div class="topbar">
    <div class="tb-rot" aria-live="polite">
      <span class="tb-item on"><svg class="ic"><use href="#i-spark"/></svg> <b>3 fragrâncias</b> em um kit só</span>
      <span class="tb-item"><svg class="ic"><use href="#i-vol"/></svg> Fixação de <b>3 a 4 horas</b> · reaplique quando quiser</span>
      <span class="tb-item"><svg class="ic"><use href="#i-truck"/></svg> Frete Expresso <b>em até 24h</b> pra todo Brasil</span>
      <span class="tb-item"><svg class="ic"><use href="#i-shield"/></svg> <b>7 dias</b> de garantia · troca sem burocracia</span>
    </div>
  </div>

  <!-- ================ NAV ================ -->
  <nav class="nav">
    <div class="wrap">
      <div class="brand">{SITE.storeName}<span>.</span></div>
    </div>
  </nav>

  <!-- ================ HERO ================ -->
  <section class="hero"><div class="wrap"><div class="hero-grid">
    <div class="hero-img-wrap">
      <span class="disc-badge"><b id="disc-badge">-{initialDiscount}%</b><span>Off hoje</span></span>
      <img src={heroImage.src} width={heroImage.width} height={heroImage.height} alt={SITE.productName} loading="eager">
    </div>
    <div>
      <div class="hero-rating reveal">
        <span class="hr-score">4,8</span>
        <span class="hr-mid">
          <span class="stars">
            <svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg>
          </span>
          <span class="hr-meta"><b>312</b> avaliações · <b>4,1 mil</b> vendidos</span>
        </span>
      </div>
      <h1 class="h1">Perfumaria de verdade, sem o preço de perfumaria.<br>3 fragrâncias premium a partir de <em>R$ 13,32 cada</em></h1>
      <p class="sub">Vibration pra chamar atenção de dia, Blunn pra fechar negócio com estilo, Infalível Fero pra arrasar à noite. Fixação de verdade: nada de cheiro fraco que some rápido.</p>
      <div class="kit-lbl" id="hero-kit">{initialKit.name} · R$ {brl(initialKit.perUnit)} por frasco</div>
      <div class="price-row">
        <span class="price-now" id="hero-price">R$ {brl(initialKit.price)}</span>
        <span class="price-old" id="hero-old">R$ {brl(initialKit.original)}</span>
      </div>
      <div class="price-hint"><svg class="ic"><use href="#i-pix"/></svg> Pagamento à vista no PIX · liberação na hora</div>

      <div class="ksel">
        <div class="of-kits" id="ofKits" role="radiogroup" aria-label="Escolha a quantidade de frascos">
          {KITS.map((k) => (
            <button type="button" role="radio" aria-checked={k.i === DEFAULT_KIT_INDEX ? "true" : "false"} class:list={["of-kit", { on: k.i === DEFAULT_KIT_INDEX }]} data-i={k.i}>
              {k.badge && <span class="tag">{k.badge}</span>}
              <span class="radio"></span>
              <span class:list={["imgs", { "imgs-6": k.qty >= 6, "imgs-1": k.qty === 1 }]}>
                {k.qty >= 6 ? (
                  <img src={kit6Image.src} alt="" loading="lazy">
                ) : (k.qty === 1 ? [vibrationThumb] : kitThumbs).map((img) => (
                  <img src={img.src} alt="" loading="lazy">
                ))}
              </span>
              <span class="kinfo">
                <span class="kname">{k.name}</span>
                <span class="kper" data-per={k.i}>{k.hint}</span>
              </span>
              <span class="kprice">
                <b data-price={k.i}>R$ {brl(k.price)}</b>
                <s data-old={k.i}>R$ {brl(k.original)}</s>
              </span>
            </button>
          ))}
        </div>
      </div>

      <button data-open-checkout class="btn big pulse buycta" type="button">QUERO O MEU AGORA</button>

      <div class="trust-row">
        <div><span class="ico"><svg class="ic"><use href="#i-lock"/></svg></span> Pagamento seguro</div>
        <div><span class="ico"><svg class="ic"><use href="#i-truck"/></svg></span> Envio em 24h</div>
        <div><span class="ico"><svg class="ic"><use href="#i-shield"/></svg></span> 7 dias de garantia</div>
        <div><span class="ico"><svg class="ic"><use href="#i-zap"/></svg></span> Aprovação na hora</div>
      </div>
    </div>
  </div></div></section>

  <!-- ================ VIDEO MARQUEE ================ -->
  <section class="sec"><div class="wrap">
    <h2 class="reveal">Quem já usou, mostrou</h2>
    <p class="lead reveal">Vídeos reais de clientes que compraram {SITE.productName}</p>
  </div>
  <div class="vmarquee"><div class="vtrack" id="vtrack">
    <div class="vidcard"><video src="/videos/showcase-1.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-2.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-3.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-4.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-5.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-6.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-7.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-8.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-1.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-2.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-3.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-4.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-5.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-6.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-7.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
    <div class="vidcard"><video src="/videos/showcase-8.mp4" muted loop playsinline preload="metadata"></video><span class="cap"><span class="cap-top">Feedback real <span class="cap-vf"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span></span><span class="cap-fol">Vídeo enviado por cliente</span></span></div>
  </div></div>
  </section>

  <!-- ================ 3 FRAGRÂNCIAS DO KIT ================ -->
  <section class="sec scn" id="scn-passos" style="--a1: 1; --a2: 0; --a3: 0;"><div class="wrap">
    <h2 class="reveal">Um kit, três armas de sedução</h2>
    <p class="lead reveal">Toque nos números e escolhe a sua</p>
  </div>
    <div class="sv ps-sv">
      <div class="ps-tint ps-tint-1"></div>
      <div class="ps-tint ps-tint-2"></div>
      <div class="ps-tint ps-tint-3"></div>
      <div class="ps-rail">
        <button type="button" class="ps-dot on" data-dot="0" aria-label="Vibration: frescor cítrico">1</button>
        <span class="ps-rail-track"><i class="ps-rail-fill" id="psFill1" style="--f: 0;"></i></span>
        <button type="button" class="ps-dot" data-dot="1" aria-label="Blunn: elegância amadeirada">2</button>
        <span class="ps-rail-track"><i class="ps-rail-fill" id="psFill2" style="--f: 0;"></i></span>
        <button type="button" class="ps-dot" data-dot="2" aria-label="Infalível Fero: aquático moderno">3</button>
      </div>
      <div class="ps-act ps-act-1">
        <span class="ps-eyebrow"><b>1</b> Frescor vibrante</span>
        <div class="ps-fig">
          <i class="ps-glow"></i>
          <i class="ps-ring"></i>
          <i class="ps-part ps-pa"></i><i class="ps-part ps-pb"></i><i class="ps-part ps-pc"></i><i class="ps-part ps-pd"></i>
          <span class="ps-tag ps-tag-l"><b>Inspirado no Vibrato</b>toranja, bergamota e gengibre</span>
          <span class="ps-tag ps-tag-r"><b>Melhor pra</b>o dia a dia e a rotina de trabalho</span>
          <img src={vibrationImage.src} alt="Body Man Vibration" loading="lazy">
        </div>
        <h3>Body Man <em>VIBRATION</em>.</h3>
        <p>Toranja, bergamota e gengibre batendo forte: frescor que anuncia que você chegou antes de você falar oi.</p>
        <div class="ps-chips"><span>Cítrico</span><span>Toranja & gengibre</span><span>Uso diário</span></div>
      </div>
      <div class="ps-act ps-act-2">
        <span class="ps-eyebrow"><b>2</b> Elegância aromática</span>
        <div class="ps-fig">
          <i class="ps-glow"></i>
          <i class="ps-ring"></i>
          <i class="ps-part ps-pa"></i><i class="ps-part ps-pb"></i><i class="ps-part ps-pc"></i><i class="ps-part ps-pd"></i>
          <span class="ps-tag ps-tag-l"><b>Inspirado no Bleu de Chanel</b>perfil cítrico e amadeirado</span>
          <span class="ps-tag ps-tag-r"><b>Melhor pra</b>reuniões e ocasiões sofisticadas</span>
          <img src={blunnImage.src} alt="Body Man Blunn" loading="lazy">
        </div>
        <h3>Body Man <em>BLUNN</em>.</h3>
        <p>Cítrico e amadeirado que grita autoridade sem precisar levantar a voz: o cheiro de quem já sabe o que quer.</p>
        <div class="ps-chips"><span>Amadeirado</span><span>Inspirado no Bleu de Chanel</span><span>Sofisticado</span></div>
      </div>
      <div class="ps-act ps-act-3">
        <span class="ps-eyebrow"><b>3</b> Frescor aquático</span>
        <div class="ps-fig">
          <i class="ps-glow"></i>
          <i class="ps-ring"></i>
          <i class="ps-part ps-pa"></i><i class="ps-part ps-pb"></i><i class="ps-part ps-pc"></i><i class="ps-part ps-pd"></i>
          <span class="ps-tag ps-tag-l"><b>Criação autoral</b>bergamota, limão e algas marinhas</span>
          <span class="ps-tag ps-tag-r"><b>Melhor pra</b>a noite e ocasiões especiais</span>
          <img src={feroImage.src} alt="Body Man Infalível Fero" loading="lazy">
        </div>
        <h3>Body Man <em>INFALÍVEL FERO</em>.</h3>
        <p>Bergamota, limão e algas marinhas sobre almíscar e cedro: o tipo de cheiro que faz ela chegar mais perto pra confirmar o que sentiu.</p>
        <div class="ps-chips"><span>Aquático</span><span>Almíscar & cedro</span><span>Criação autoral</span></div>
      </div>
    </div>
  </section>

  <!-- ================ VIDEO PRINCIPAL ================ -->
  <section class="mainvid"><div class="wrap">
    <div class="mv-grid">
      <div class="mv-side">
        <h2 class="reveal">Quem já borrifou, aprovou</h2>
        <p class="lead reveal">Um kit pra alternar de fragrância conforme o momento, do dia a dia às ocasiões especiais.</p>
        <div class="mv-quotes reveal">
          <div class="mvq tilt-card"><span class="qic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg></span><p>"Fixou o dia inteiro. Recebi elogio até de quem <b>nunca fala nada</b>."</p></div>
          <div class="mvq tilt-card"><span class="qic"><svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"/></svg></span><p>Troco de fragrância que nem troco de roupa, <b>nunca sou "aquele cheiro de sempre"</b>.</p></div>
          <div class="mvq tilt-card"><span class="qic"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg></span><p>Borrifo e esqueço: o cheiro que <b>continua trabalhando</b> por mim o dia todo.</p></div>
        </div>
      </div>
      <div class="box" id="mv-box">
        <video id="mv-video" src="/main-testimonial.mp4" muted autoplay loop playsinline preload="metadata"></video>
        <button class="mv-play" id="mv-play" aria-label="Assistir com som" type="button">
          <span class="mv-ico"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></span>
          <span class="mv-lbl"><svg class="ic"><use href="#i-vol"/></svg> Toque para ouvir</span>
        </button>
        <span class="vbadge vb1"><span class="vb-glass"><span class="vb-ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg></span><b>Fixação<br>de até 4h</b></span></span>
        <span class="vbadge vb2"><span class="vb-glass"><span class="vb-ic"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"/></svg></span><b>Sente<br>na hora</b></span></span>
        <span class="vbadge vb3"><span class="vb-glass"><span class="vb-ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg></span><b>Uso<br>diário</b></span></span>
      </div>
    </div>
    <p class="hint"><svg class="ic"><use href="#i-vol"/></svg> Toque no vídeo pra ouvir com som</p>
  </div></section>

  <!-- ================ OCASIÕES IDEAIS ================ -->
  <section class="sec" id="scn-ocasioes"><div class="wrap">
    <h2 class="reveal">Pra cada ocasião, uma fragrância</h2>
    <p class="lead reveal">Versatilidade pra sua rotina, do dia a dia às ocasiões especiais</p>
    <div class="occ-grid">
      <div class="occ reveal"><svg class="ic"><use href="#i-zap"/></svg><span>Dia a dia</span></div>
      <div class="occ reveal"><svg class="ic"><use href="#i-coffee"/></svg><span>Trabalho</span></div>
      <div class="occ reveal"><svg class="ic"><use href="#i-vol"/></svg><span>Pós-banho</span></div>
      <div class="occ reveal"><svg class="ic"><use href="#i-leaf"/></svg><span>Lazer</span></div>
      <div class="occ reveal"><svg class="ic"><use href="#i-star"/></svg><span>Ocasiões especiais</span></div>
    </div>
    <p class="ba2-note">Borrife a cerca de 15cm da pele, aguarde a saída alcoólica se dissipar e reaplique ao longo do dia sempre que quiser renovar o aroma. Contém álcool. Fixação média de 3 a 4 horas, podendo variar conforme pele, clima e rotina de uso.</p>
  </div></section>

  <!-- ================ DEPOIMENTOS ================ -->
  <section id="avaliacoes" class="sec" style="background:#fff;border-block:1px solid var(--border)"><div class="wrap">
    <h2 class="reveal">Quem usou, aprovou</h2>
    <p class="lead reveal">Avaliações verificadas de compradores reais</p>

    <div class="revs-grid">
      <div class="rev rev-photo reveal tilt-card">
        <div class="top">
          <span class="nm">a**a</span>
          <span class="vf"><svg class="ic" style="width:11px;height:11px"><use href="#i-check"/></svg> Compra verificada</span>
        </div>
        <span class="stars"><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg></span>
        <p>"Cara, não sei o que colocaram nesse troço, mas as mulheres do meu prédio ficam me parando no elevador só pra perguntar o perfume. Tô rindo, mas tô amando kkkk."</p>
        <img class="rev-pic" src={cliente1.src} width={cliente1.width} height={cliente1.height} alt="Foto anexada por a**a" loading="lazy">
      </div>
      <div class="rev rev-photo reveal tilt-card">
        <div class="top">
          <span class="nm">m**a s**s</span>
          <span class="vf"><svg class="ic" style="width:11px;height:11px"><use href="#i-check"/></svg> Compra verificada</span>
        </div>
        <span class="stars"><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg></span>
        <p>"Borrifei de manhã, o cheiro ainda tava lá de noite. Pra mim isso já vale o preço todo."</p>
        <img class="rev-pic" src={cliente2.src} width={cliente2.width} height={cliente2.height} alt="Foto anexada por m**a s**s" loading="lazy">
      </div>
      <div class="rev rev-photo reveal tilt-card">
        <div class="top">
          <span class="nm">r**o</span>
          <span class="vf"><svg class="ic" style="width:11px;height:11px"><use href="#i-check"/></svg> Compra verificada</span>
        </div>
        <span class="stars"><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg></span>
        <p>"Um colega perguntou se eu tinha trocado pra um importado. Só sorri e não respondi nada. Segredo é segredo."</p>
        <img class="rev-pic" src={cliente4.src} width={cliente4.width} height={cliente4.height} alt="Foto anexada por r**o" loading="lazy">
      </div>
      <div class="rev rev-photo reveal tilt-card">
        <div class="top">
          <span class="nm">c**a p**a</span>
          <span class="vf"><svg class="ic" style="width:11px;height:11px"><use href="#i-check"/></svg> Compra verificada</span>
        </div>
        <span class="stars"><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg><svg class="ic"><use href="#i-star"/></svg></span>
        <p>"Comprei desconfiado, achei que ia ser água com cheiro. Ledo engano. Hoje é chacota no grupo da academia, todo mundo quer saber a marca kkkk."</p>
        <img class="rev-pic" src={cliente3.src} width={cliente3.width} height={cliente3.height} alt="Foto anexada por c**a p**a" loading="lazy">
      </div>
    </div>
  </div></section>

  <!-- ================ ANCORA DE VALOR ================ -->
  <section class="sec value scn vl" id="scn-valor" style="--b1: 1; --b2: 0;"><div class="wrap">
    <div class="val-hero reveal">
      <span class="vh-eyebrow">Ficar cheiroso todo santo dia custa</span>
      <span class="vh-num"><small>R$</small><span class="vh-num-val" data-countup="0.38">0,38</span><small>/dia</small></span>
      <span class="vh-sub">Menos que uma bala de hortelã, e o efeito <b>não passa despercebido</b>.</span>
      <span class="vh-rule" aria-hidden="true"></span>
    </div>
    <div class="sv vl-sv">
      <div class="vlb" style="--bv:var(--b1,0)">
        <span class="vl-ico"><svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 3l1.9 5.1L19 10l-5.1 1.9L12 17l-1.9-5.1L5 10l5.1-1.9zM19 15l.9 2.1L22 18l-2.1.9L19 21l-.9-2.1L16 18l2.1-.9z"/></svg></span>
        <b>R$ 400+</b>
        <span class="vl-lab">custa 1 perfume importado</span>
        <p>E some rápido do corpo. O kit Body Man traz 600ml em 3 fragrâncias por uma fração do preço, e fixa de verdade.</p>
      </div>
      <div class="vlb" style="--bv:var(--b2,0)">
        <span class="vl-ico"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 2l8 3v6c0 5-3.4 9.2-8 11-4.6-1.8-8-6-8-11V5z"/><path d="M8.5 12l2.5 2.5 4.5-4.5"/></svg></span>
        <b>Zero</b>
        <span class="vl-lab">risco pra você</span>
        <p>7 dias de garantia: se não amar o resultado, devolvemos 100% do valor do produto.</p>
      </div>
      <div class="vl-dots">
        <button type="button" class="on" data-vl="0" aria-label="Custa menos que um clareamento a laser"></button>
        <button type="button" data-vl="1" aria-label="Risco zero"></button>
      </div>
    </div>
  </div></section>

  <!-- ================ FAQ ================ -->
  <section id="faq" class="sec" style="background:#fff;border-block:1px solid var(--border)"><div class="wrap">
    <h2 class="reveal">Dúvidas frequentes</h2>
    <p class="lead reveal">A gente responde antes de você perguntar</p>
    <div class="faq-list">
      <details class="faq-item reveal">
        <summary>Tem uma fragrância pra cada ocasião?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>Sim, o kit reúne três perfis diferentes: Vibration (cítrico, ideal pro dia a dia), Blunn (amadeirado e sofisticado, ideal pro trabalho) e Infalível Fero (aquático, ideal pra noite e ocasiões especiais). Dá pra alternar conforme o momento.</p></div>
      </details>
      <details class="faq-item reveal">
        <summary>Em quantos dias meu pedido chega?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>O Frete Expresso entrega em até 7 dias úteis para todo o Brasil, com código de rastreio. O envio começa em até 24h úteis após o pagamento aprovado.</p></div>
      </details>
      <details class="faq-item reveal">
        <summary>Posso trocar ou devolver se não gostar?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>Sim, você tem 7 dias corridos a partir do recebimento para trocar ou devolver, sem burocracia. O reembolso é integral (produto + frete pago).</p></div>
      </details>
      <details class="faq-item reveal">
        <summary>Qual a diferença entre os kits de 1, 3 e 6 frascos?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>É a quantidade de frascos de 200ml e o preço por unidade: no de 1 frasco você escolhe a fragrância no checkout; o de 3 traz uma de cada (R$22,66 por frasco); o de 6 traz duas de cada e sai ainda mais barato (R$13,32 por frasco). Os dois maiores já vêm com frete grátis.</p></div>
      </details>
      <details class="faq-item reveal">
        <summary>Quais são as formas de pagamento?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>Aceitamos Pix, com o desconto da oferta atual já aplicado no checkout. A aprovação acontece em segundos e o pedido já entra pra separação.</p></div>
      </details>
      <details class="faq-item reveal">
        <summary>Como uso o body splash e quanto tempo fixa?<svg class="ic faq-ic"><use href="#i-plus"/></svg></summary>
        <div class="faq-body"><p>Borrife diretamente sobre a pele, a cerca de 15cm de distância, e aguarde alguns instantes para a saída alcoólica se dissipar. Fixação média de 3 a 4 horas. Reaplique ao longo do dia sempre que quiser renovar o aroma. Contém álcool, característica comum a produtos desta categoria.</p></div>
      </details>
    </div>
  </div></section>

  <!-- ================ GARANTIA + CTA ================ -->
  <section class="sec"><div class="wrap"><div class="guar-wrap">
    <div class="guar reveal">
      <div class="seal"><svg class="ic"><use href="#i-shield"/></svg></div>
      <h3>Não amou? <span>Dinheiro de volta</span></h3>
      <p>Use o kit Body Man por 7 dias. Se por qualquer motivo não amar as fragrâncias, devolvemos <b>100% do valor pago</b>, sem burocracia, sem interrogatório. O risco é todo nosso.</p>
    </div>
  </div>
  <div style="text-align:center;margin-top:22px">
    <button data-open-checkout class="btn buycta" type="button" style="padding:17px 40px">Garantir meu kit agora</button>
  </div>
  </div></section>


  <!-- ================ FOOTER ================ -->
  <footer class="ft">
    <div class="wrap ft-grid">
      <div>
        <div class="brand">{SITE.storeName}<span>.</span></div>
        <p>Fragrâncias masculinas premium, entregues em todo Brasil com envio expresso.</p>
        <div class="ft-badges">
          <span><svg class="ic" style="width:13px;height:13px"><use href="#i-pix"/></svg> PIX</span>
          <span><svg class="ic" style="width:13px;height:13px"><use href="#i-lock"/></svg> Site seguro</span>
          <span><svg class="ic" style="width:13px;height:13px"><use href="#i-shield"/></svg> 7 dias de garantia</span>
        </div>
      </div>
      <div>
        <h4>Atendimento</h4>
        <a href="https://wa.me/5547945612332" target="_blank" rel="noopener"><svg class="ic" style="width:15px;height:15px"><use href="#i-chat"/></svg> WhatsApp (47) 94561-2332</a>
        <a href="mailto:atendimento@bodyman.cloud"><svg class="ic" style="width:15px;height:15px"><use href="#i-check"/></svg> atendimento@bodyman.cloud</a>
        <a href="/rastrear-pedido"><svg class="ic" style="width:15px;height:15px"><use href="#i-truck"/></svg> Rastrear meu pedido</a>
        <a href="#faq"><svg class="ic" style="width:15px;height:15px"><use href="#i-chat"/></svg> Dúvidas frequentes</a>
      </div>
      <div>
        <h4>Institucional</h4>
        <a href="/politica-de-privacidade"><svg class="ic" style="width:15px;height:15px"><use href="#i-check"/></svg> Política de Privacidade</a>
        <a href="/trocas-e-devolucoes"><svg class="ic" style="width:15px;height:15px"><use href="#i-check"/></svg> Trocas e Devoluções</a>
        <a href="/termos-de-uso"><svg class="ic" style="width:15px;height:15px"><use href="#i-check"/></svg> Termos de Uso</a>
      </div>
    </div>
    <div class="ft-bottom">
      <span>© {new Date().getFullYear()} {SITE.storeName} · Todos os direitos reservados</span>
      <span>Fragrâncias masculinas premium</span>
    </div>
  </footer>

  <!-- ================ STICKY CTA ================ -->
  <div class="stickycta"><div class="wrap">
    <div class="sk-kits" role="radiogroup" aria-label="Escolha a quantidade de frascos">
      {KITS.map((k) => (
        <button type="button" role="radio" aria-checked={k.i === DEFAULT_KIT_INDEX ? "true" : "false"} class:list={["sk-kit", { on: k.i === DEFAULT_KIT_INDEX }]} data-idx={k.i}>
          <span>{k.qty} Frasco{k.qty > 1 ? "s" : ""}</span>
          <b>R$ {brl(k.price)}</b>
        </button>
      ))}
    </div>
    <button data-open-checkout class="btn buycta" type="button">Comprar</button>
  </div></div>

  <CheckoutModal />

  <script type="application/ld+json" set:html={JSON.stringify(productSchema)} is:inline />
</Layout>

<!-- ============================================================
     ESTILO — CSS da PV. Paleta vem de src/styles/global.css (var(--wine),
     var(--gold), var(--pix), etc — --wine/--ac/--ac-d apelidados aqui pra
     superfícies preenchidas, --gold usado direto pra texto/borda sobre o
     fundo escuro desta página).
     ============================================================ -->
<style is:global>
  /* Paleta herdada de src/styles/global.css (--wine/--gold/--pix/--cream/--blush/--star) —
     NÃO redefina cores aqui. Pra rebrandear pra outro produto, edite só o
     bloco "PALETA DO PRODUTO" em global.css; estes tokens são só apelidos
     usados pelo CSS desta página. --ink/--muted/--bg/--border/--porc/--serif/
     --sans/--rad são neutros de tipografia/layout, não fazem parte da marca —
     opcionalmente ajustáveis, mas raramente precisam mudar entre produtos.
     Esta página roda em TEMA ESCURO: --bg/--porc são escuros e --ink/--muted
     são claros (o oposto do que o nome sugeriria numa página clara comum). */
  :root {
    color-scheme: dark;
    --ac: var(--wine);
    --ac-d: color-mix(in oklab, var(--wine) 82%, black);
    --ac-l: color-mix(in oklab, var(--gold) 12%, var(--porc));
    --ink: oklch(94% 0.012 85);
    --muted: oklch(70% 0.02 85);
    --bg: oklch(15% 0.012 85);
    --border: oklch(32% 0.018 85);
    --ok: oklch(78% 0.17 149.58);
    --serif: var(--font-display);
    --sans: var(--font-sans);
    --porc: oklch(21% 0.016 85);
    /* --blush não é redeclarado aqui — herda direto de global.css pra evitar
       ciclo de var() (self-reference deixaria a propriedade inválida). */
    --line: var(--border);
    --rad: 22px;
  }
  body { font-family: var(--sans); color: var(--ink); background: var(--bg); -webkit-font-smoothing: antialiased; overflow-x: hidden }
  img { max-width: 100%; display: block }
  .wrap { max-width: 1080px; margin: 0 auto; padding: 0 18px }
  .ic { width: 16px; height: 16px; display: inline-block; vertical-align: middle; flex: none }
  .stars { display: inline-flex; gap: 1px; color: var(--star) }
  .stars .ic { width: 15px; height: 15px }

  .btn { display: inline-flex; align-items: center; justify-content: center; gap: 9px; background: linear-gradient(135deg, var(--ac), var(--ac-d)); color: #fff; border: none; border-radius: 100px; font: 800 16px/1 var(--sans); padding: 17px 26px; cursor: pointer; box-shadow: 0 10px 24px -8px color-mix(in oklab, var(--ac) 60%, transparent); transition: transform .15s, box-shadow .15s; text-decoration: none }
  .btn:hover { transform: translateY(-2px); box-shadow: 0 14px 30px -8px color-mix(in oklab, var(--ac) 70%, transparent) }
  .btn:active { transform: translateY(0) }
  .btn.big { width: 100%; padding: 18px; font-size: 18px }
  .pulse { animation: pulse 2.2s ease-in-out infinite }
  @keyframes pulse {
    0%, 100% { box-shadow: 0 10px 24px -8px color-mix(in oklab, var(--ac) 60%, transparent), 0 0 0 0 color-mix(in oklab, var(--ac) 40%, transparent) }
    50% { box-shadow: 0 10px 24px -8px color-mix(in oklab, var(--ac) 60%, transparent), 0 0 0 14px color-mix(in oklab, var(--ac) 0%, transparent) }
  }
  /* TOPBAR */
  .topbar { background: var(--wine); color: var(--wine-foreground); font-size: 12.5px; font-weight: 700; padding: 9px 12px }
  .topbar b { color: var(--gold); font-weight: 800 }
  .tb-rot { position: relative; height: 18px; overflow: hidden; max-width: 640px; margin: 0 auto }
  .tb-item { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; gap: 7px; white-space: nowrap; opacity: 0; transform: translateY(-110%); transition: opacity .5s ease, transform .55s cubic-bezier(.16, 1, .3, 1) }
  .tb-item.on { opacity: 1; transform: translateY(0) }
  .tb-item.out { opacity: 0; transform: translateY(110%) }
  .tb-item .ic { width: 14px; height: 14px; flex: 0 0 auto }

  .nav { position: sticky; top: 0; z-index: 40; background: color-mix(in oklab, var(--bg) 88%, transparent); backdrop-filter: blur(10px); border-bottom: 1px solid var(--border) }
  .nav .wrap { display: flex; align-items: center; justify-content: center; height: 62px }
  .brand { font-family: var(--serif); font-size: 27px; font-weight: 600; letter-spacing: -.02em }
  .brand span { color: var(--gold) }

  /* HERO */
  .hero { padding: 24px 0 8px }
  .hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 34px; align-items: center }
  @media (max-width: 680px) { .hero-grid { grid-template-columns: 1fr; gap: 14px } }
  @media (min-width: 681px) and (max-width: 900px) { .hero-grid { grid-template-columns: .82fr 1fr; gap: 22px } }
  .hero-img-wrap { position: relative }
  .hero-img-wrap::before {
    content: ""; position: absolute; inset: -8%; z-index: 0; border-radius: 50%;
    background: radial-gradient(ellipse at center, color-mix(in oklab, var(--gold) 16%, transparent) 0%, transparent 70%);
  }
  .hero-img-wrap img {
    position: relative; z-index: 1;
    width: 100%; border-radius: 22px; aspect-ratio: 1/1; object-fit: cover;
    outline: 1px solid color-mix(in oklab, var(--gold) 25%, transparent); outline-offset: -1px;
    box-shadow:
      0 2px 4px -2px color-mix(in oklab, var(--gold) 30%, transparent),
      0 12px 20px -10px color-mix(in oklab, var(--gold) 35%, transparent),
      0 32px 64px -28px color-mix(in oklab, var(--gold) 40%, transparent);
  }
  .disc-badge { position: absolute; top: 14px; left: 14px; z-index: 2; width: 68px; height: 68px; border-radius: 50%; background: linear-gradient(140deg, var(--ac), var(--ac-d)); color: #fff; display: flex; flex-direction: column; align-items: center; justify-content: center; box-shadow: 0 12px 26px -8px color-mix(in oklab, var(--ac) 75%, transparent); border: 2px solid color-mix(in oklab, var(--gold) 55%, transparent) }
  .disc-badge b { font-family: var(--serif); font-size: 20px; font-weight: 600; line-height: 1 }
  .disc-badge span { font-size: 8px; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; margin-top: 2px; opacity: .95 }
  .rate-row { display: flex; align-items: center; gap: 8px; font-size: 13px; color: var(--muted); font-weight: 600; margin-bottom: 10px; flex-wrap: wrap }
  .h1 { font-family: var(--serif); font-size: 34px; line-height: 1.06; font-weight: 600; letter-spacing: -.02em; margin-bottom: 8px }
  .h1 em { color: var(--gold); font-style: italic }
  @media (max-width: 680px) { .h1 { font-size: 27px } }
  @media (min-width: 681px) and (max-width: 900px) { .h1 { font-size: 29px } }
  .sub { font-size: 14.5px; color: var(--muted); line-height: 1.45; margin-bottom: 14px; max-width: 46ch }
  .kit-lbl { font-size: 12px; font-weight: 800; color: var(--gold); text-transform: uppercase; letter-spacing: .05em; margin-bottom: 2px }
  .price-row { display: flex; align-items: baseline; gap: 12px }
  .price-now { font-family: var(--serif); font-size: 42px; font-weight: 600; color: var(--gold); letter-spacing: -.02em }
  .price-old { font-size: 18px; color: var(--muted); text-decoration: line-through }
  .price-hint { font-size: 12.5px; color: var(--ok); font-weight: 700; margin: 3px 0 14px; display: flex; align-items: center; gap: 6px }

  /* KIT SELECTOR (do modelo 62 do template) */
  .ksel { margin: 10px 0 22px }
  .of-kits { display: flex; flex-direction: column; gap: 10px }
  .of-kit { position: relative; display: flex; align-items: center; gap: 12px; padding: 14px; background: var(--porc); border: 2px solid var(--border); border-radius: 16px; cursor: pointer; text-align: left; font-family: inherit; transition: .18s; width: 100% }
  .of-kit:hover { border-color: var(--gold) }
  .of-kit.on { border-color: var(--gold); background: linear-gradient(180deg, var(--ac-l), var(--porc)); box-shadow: 0 12px 26px -12px color-mix(in oklab, var(--ac) 60%, transparent) }
  .of-kit .radio { width: 20px; height: 20px; border-radius: 50%; border: 2px solid var(--border); flex: none; position: relative; transition: .2s }
  .of-kit.on .radio { border-color: var(--ac); background: var(--ac) }
  .of-kit.on .radio::after { content: ""; position: absolute; inset: 4px; background: #fff; border-radius: 50% }
  .of-kit .imgs { display: flex; gap: 2px; flex: none }
  .of-kit .imgs img { width: 34px; height: 44px; object-fit: cover; border-radius: 6px; border: 1px solid var(--border); background: #fff }
  .of-kit .imgs img:nth-child(2) { margin-left: -12px }
  .of-kit .imgs img:nth-child(3) { margin-left: -12px }
  .of-kit .imgs.imgs-6 img { width: 78px; height: 44px; object-fit: cover; object-position: center 30% }
  .of-kit .imgs.imgs-1 img { width: 38px; height: 49px; margin: 0 8px 0 0 }
  @keyframes price-pop { 0% { transform: scale(1) } 45% { transform: scale(1.1) } 100% { transform: scale(1) } }
  .price-pop { display: inline-block; animation: price-pop .4s cubic-bezier(.16,1,.3,1) }
  @media (prefers-reduced-motion: reduce) { .price-pop { animation: none } }
  .of-kit .kinfo { flex: 1; min-width: 0 }
  .of-kit .kname { display: block; font-weight: 800; font-size: 14px; color: var(--ink); line-height: 1.15 }
  .of-kit .kper { display: block; font-size: 11.5px; color: var(--muted); font-weight: 600; margin-top: 2px }
  .of-kit .kprice { text-align: right; flex: none }
  .of-kit .kprice b { display: block; font-family: var(--serif); font-size: 18px; font-weight: 600; color: var(--gold); line-height: 1 }
  .of-kit .kprice s { display: block; font-size: 11px; color: var(--muted); margin-top: 2px }
  .of-kit .tag { position: absolute; top: -8px; left: 14px; background: linear-gradient(135deg, var(--ac), var(--ac-d)); color: #fff; font-size: 9px; font-weight: 800; letter-spacing: .06em; padding: 3px 10px; border-radius: 100px; box-shadow: 0 5px 12px -3px color-mix(in oklab, var(--ac) 85%, transparent); text-transform: uppercase }


  .trust-row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px 8px; margin-top: 16px }
  .trust-row div { display: flex; align-items: center; justify-content: center; gap: 8px; font-size: 12px; color: var(--muted); font-weight: 600 }
  .trust-row .ico { width: 28px; height: 28px; border-radius: 8px; background: var(--ac-l); display: flex; align-items: center; justify-content: center; color: var(--gold) }

  .sec { padding: 42px 0 }
  .sec h2 { font-family: var(--serif); font-size: 28px; font-weight: 600; text-align: center; letter-spacing: -.02em; margin-bottom: 6px }
  .sec p.lead { text-align: center; color: var(--muted); font-size: 14.5px; margin-bottom: 22px }

  /* OCASIÕES IDEAIS */
  .occ-grid { display: flex; flex-wrap: wrap; justify-content: center; gap: 12px; max-width: 640px; margin: 0 auto 22px }
  .occ { display: flex; flex-direction: column; align-items: center; gap: 8px; width: 108px; padding: 16px 10px; border-radius: 16px; background: var(--porc); border: 1px solid var(--border); box-shadow: 0 10px 24px -18px color-mix(in oklab, var(--gold) 14%, transparent) }
  .occ .ic { width: 22px; height: 22px; color: var(--gold) }
  .occ span { font-size: 12px; font-weight: 700; color: var(--ink); text-align: center; line-height: 1.25 }

  /* VIDEO MARQUEE */
  .vmarquee { overflow: hidden; position: relative; padding: 6px 0; -webkit-mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent); mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent) }
  .vtrack { display: flex; width: max-content; animation: vscroll 54s linear infinite }
  @keyframes vscroll { from { transform: translateX(0) } to { transform: translateX(-50%) } }
  .vmarquee:hover .vtrack { animation-play-state: paused }
  .vidcard { flex: none; width: 186px; margin-right: 14px; border-radius: 16px; overflow: hidden; position: relative; background: #000; aspect-ratio: 9/16; box-shadow: 0 16px 34px -20px rgba(0, 0, 0, .5) }
  .vidcard video { width: 100%; height: 100%; object-fit: cover; display: block }
  .vidcard .cap { position: absolute; left: 11px; right: 11px; bottom: 11px; color: #fff; text-shadow: 0 1px 6px rgba(0, 0, 0, .92); display: flex; flex-direction: column; gap: 1px }
  .vidcard .cap-top { display: flex; align-items: center; gap: 4px; font-size: 13px; font-weight: 800; line-height: 1.15 }
  .vidcard .cap-vf { display: inline-flex; flex: none; width: 15px; height: 15px }
  .vidcard .cap-vf svg { width: 15px; height: 15px; filter: drop-shadow(0 1px 2px rgba(0, 0, 0, .35)) }
  .vidcard .cap-fol { font-size: 10.5px; font-weight: 700; opacity: .93 }

  /* 3 PASSOS */
  .scn { position: relative; --p: 0; background: var(--porc); margin: 0 auto }
  .scn h1, .scn h2, .scn h3 { font-family: var(--serif); font-weight: 600; line-height: 1.12; letter-spacing: -.01em }
  .sv { position: relative; width: min(100%, 560px); margin: 0 auto; height: min(78svh, 600px); overflow: hidden; border-radius: 24px; display: flex; flex-direction: column; align-items: center; justify-content: center }
  #scn-passos { padding: 42px 16px 0 }
  .ps-sv { background: var(--porc); box-shadow: 0 20px 50px -24px color-mix(in oklab, var(--gold) 30%, transparent) }
  .ps-tint { position: absolute; inset: 0; pointer-events: none; transition: opacity .5s ease }
  .ps-tint-1 { background: radial-gradient(75% 60% at 50% 8%, color-mix(in oklab, var(--gold) 10%, var(--porc)) 0%, transparent 75%); opacity: var(--a1, 0) }
  .ps-tint-2 { background: radial-gradient(75% 60% at 50% 8%, color-mix(in oklab, var(--gold) 16%, var(--porc)) 0%, transparent 75%); opacity: var(--a2, 0) }
  .ps-tint-3 { background: radial-gradient(75% 60% at 50% 8%, color-mix(in oklab, var(--gold) 8%, var(--porc)) 0%, transparent 75%); opacity: var(--a3, 0) }
  .ps-rail { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); z-index: 5; display: flex; flex-direction: column; align-items: center; gap: 0; height: 200px }
  .ps-rail-track { position: relative; width: 2px; flex: 1; background: var(--line); border-radius: 2px; overflow: hidden }
  .ps-rail-fill { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: var(--gold); transform-origin: top; transform: scaleY(var(--f, 0)); transition: transform .45s ease }
  /* Visual continua 26px (não cabe maior no rail), mas o ::before estende a
     área de toque real pra 44x44 — mesma técnica do .vl-dots abaixo. */
  .ps-dot { position: relative; width: 26px; height: 26px; border-radius: 50%; background: var(--porc); border: 1.5px solid var(--line); display: grid; place-items: center; font-size: .62rem; font-weight: 800; color: var(--muted); font-family: var(--sans); z-index: 2; transition: all .3s; cursor: pointer; padding: 0 }
  .ps-dot::before { content: ""; position: absolute; top: 50%; left: 50%; width: 44px; height: 44px; transform: translate(-50%, -50%) }
  .ps-dot.on { background: var(--ac); border-color: var(--ac); color: #fff; box-shadow: 0 0 0 5px color-mix(in oklab, var(--ac) 18%, transparent) }
  .ps-act { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 34px 30px 30px 52px; gap: 10px; opacity: var(--av, 0); transform: translateX(calc((1 - var(--av, 0)) * var(--d, 1) * 62%)); pointer-events: none; transition: opacity .5s cubic-bezier(.4, 0, .2, 1), transform .55s cubic-bezier(.16, 1, .3, 1) }
  .ps-act-1 { --av: var(--a1, 0); --d: var(--d1, 1) } .ps-act-2 { --av: var(--a2, 0); --d: var(--d2, 1) } .ps-act-3 { --av: var(--a3, 0); --d: var(--d3, 1) }
  .ps-eyebrow { font-size: .66rem; font-weight: 800; letter-spacing: .2em; text-transform: uppercase; color: var(--gold) }
  .ps-eyebrow b { display: inline-grid; place-items: center; width: 22px; height: 22px; border-radius: 50%; background: var(--ac); color: #fff; font-size: .7rem; margin-right: 6px; vertical-align: middle }
  .ps-act img { width: min(40vw, 190px); height: min(34svh, 240px); object-fit: contain; filter: drop-shadow(0 20px 30px color-mix(in oklab, var(--gold) 25%, transparent)); transform: scale(calc(.88 + var(--av, 0)*.12)) rotateY(calc((1 - var(--av, 0)) * var(--d, 1) * -22deg)) }
  .ps-fig { position: relative; display: grid; place-items: center; width: 100%; perspective: 900px }
  .ps-fig img { position: relative; z-index: 2 }
  .ps-glow { position: absolute; width: min(58vw, 300px); aspect-ratio: 1; border-radius: 50%; background: radial-gradient(circle, rgba(255, 255, 255, .95) 0%, rgba(255, 255, 255, 0) 68%) }
  .ps-fig::after { content: ""; position: absolute; bottom: -8px; width: 36%; height: 14px; border-radius: 50%; background: radial-gradient(ellipse, color-mix(in oklab, var(--gold) 35%, transparent), transparent 70%); filter: blur(2px); transform: scaleX(calc(.7 + var(--av, 0)*.3)) }
  .ps-part { position: absolute; border-radius: 50%; pointer-events: none; z-index: 1 }
  .ps-pa { width: 13px; height: 13px; left: 15%; top: 10%; transform: translateY(calc(var(--av, 0)*-48px)) }
  .ps-pb { width: 8px; height: 8px; right: 17%; top: 24%; transform: translateY(calc(var(--av, 0)*-84px)) }
  .ps-pc { width: 6px; height: 6px; left: 25%; bottom: 4%; transform: translateY(calc(var(--av, 0)*-30px)) }
  .ps-pd { width: 10px; height: 10px; right: 23%; bottom: 14%; transform: translateY(calc(var(--av, 0)*-64px)) }
  .ps-act-1 .ps-part { background: color-mix(in oklab, var(--gold) 25%, white); box-shadow: inset -2px -2px 4px color-mix(in oklab, var(--ac) 30%, transparent), 0 2px 6px rgba(0, 0, 0, .25) }
  .ps-act-2 .ps-part { background: var(--ac); opacity: .45 }
  .ps-act-3 .ps-part { background: var(--gold); opacity: .55 }
  .ps-act h3 { font-size: clamp(1.6rem, 6.6vw, 2.4rem) }
  .ps-act h3 em { color: var(--gold) }
  .ps-act p { color: var(--muted); max-width: 420px; font-size: clamp(.9rem, 3.5vw, 1.02rem) }
  .ps-chips { display: flex; flex-wrap: wrap; gap: 8px; justify-content: center; margin-top: 4px }
  .ps-chips span { padding: 6px 13px; border-radius: 100px; background: var(--porc); border: 1px solid var(--line); font-size: .7rem; font-weight: 700; color: var(--ink); box-shadow: 0 4px 10px color-mix(in oklab, var(--gold) 10%, transparent) }
  .ps-chips span::before { content: ""; display: inline-block; width: 5px; height: 5px; border-radius: 50%; background: var(--gold); margin-right: 7px; vertical-align: 2px }
  .ps-chips span:nth-child(1) { transform: translateY(calc((1 - var(--av, 0))*20px)) }
  .ps-chips span:nth-child(2) { transform: translateY(calc((1 - var(--av, 0))*36px)) }
  .ps-chips span:nth-child(3) { transform: translateY(calc((1 - var(--av, 0))*52px)) }
  @keyframes spin { to { rotate: 360deg } }
  .ps-ring { position: absolute; inset: 0; margin: auto; width: min(64vw, 320px); aspect-ratio: 1; border-radius: 50%; border: 1.5px dashed color-mix(in oklab, var(--gold) 28%, transparent); animation: spin 40s linear infinite; pointer-events: none }
  .ps-ring::before { content: ""; position: absolute; top: -5px; left: 50%; width: 9px; height: 9px; border-radius: 50%; background: var(--gold); box-shadow: 0 0 10px color-mix(in oklab, var(--gold) 80%, transparent) }
  .ps-tag { position: absolute; z-index: 3; max-width: 118px; padding: 8px 11px; border-radius: 14px; text-align: left; background: var(--porc); border: 1px solid var(--line); box-shadow: 0 10px 24px color-mix(in oklab, var(--gold) 14%, transparent); font-size: .58rem; font-weight: 600; line-height: 1.35; color: var(--muted); opacity: clamp(0, calc(var(--av, 0)*2.2 - .55), 1) }
  .ps-tag b { display: block; color: var(--gold); font-size: .72rem; margin-bottom: 2px }
  .ps-tag-l { top: 25%; left: 4% }
  .ps-tag-r { top: 25%; right: 4% }

  /* VIDEO PRINCIPAL */
  .mainvid { text-align: center; padding: 22px 0 38px; background: linear-gradient(180deg, var(--porc), var(--bg)) }
  .mainvid .box { position: relative; max-width: 420px; margin: 0 auto; border-radius: 20px; overflow: visible; box-shadow: 0 26px 54px -26px rgba(0, 0, 0, .55); background: #000 }
  .mainvid video { width: 100%; display: block; border-radius: 20px }
  .mainvid .hint { margin-top: 12px; font-size: 12.5px; color: var(--muted); font-weight: 600; display: inline-flex; align-items: center; gap: 6px }
  .mv-play { position: absolute; inset: 0; width: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 12px; border: none; cursor: pointer; background: linear-gradient(180deg, rgba(20, 10, 15, .12), rgba(20, 10, 15, .42)); transition: opacity .3s; border-radius: 20px; overflow: hidden }
  .mv-ico { width: 76px; height: 76px; border-radius: 50%; background: rgba(255, 255, 255, .96); display: flex; align-items: center; justify-content: center; box-shadow: 0 12px 30px -8px rgba(0, 0, 0, .5); animation: mvpulse 2s ease-in-out infinite }
  .mv-ico svg { width: 30px; height: 30px; color: var(--ac-d); margin-left: 4px }
  @keyframes mvpulse { 0%, 100% { transform: scale(1) } 50% { transform: scale(1.08) } }
  .mv-lbl { display: inline-flex; align-items: center; gap: 6px; color: #fff; font-size: 13px; font-weight: 800; background: rgba(0, 0, 0, .34); padding: 6px 15px; border-radius: 100px; backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px) }
  .mainvid .box.playing .mv-play { opacity: 0; pointer-events: none }
  .vbadge { position: absolute; z-index: 6; max-width: 66%; opacity: 0; transform: translateY(16px) scale(.92); animation: vb-in .7s cubic-bezier(.16, 1, .3, 1) forwards; pointer-events: none }
  .mainvid .box.playing .vbadge { opacity: 0 !important; transition: opacity .3s; animation: none }
  .vb-glass { display: inline-flex; align-items: center; gap: 9px; padding: 8px 14px 8px 8px; border-radius: 15px; background: var(--porc); border: 1px solid var(--border); box-shadow: 0 14px 34px -12px rgba(0, 0, 0, .5); animation: vbf 5.4s ease-in-out infinite }
  .vb-ic { width: 30px; height: 30px; border-radius: 10px; background: linear-gradient(140deg, var(--ac), var(--ac-d)); display: flex; align-items: center; justify-content: center; flex: 0 0 auto; box-shadow: 0 6px 14px -4px rgba(0, 0, 0, .55) }
  .vb-ic svg { width: 16px; height: 16px; color: #fff }
  .vbadge b { font-size: 12.5px; font-weight: 800; line-height: 1.12; letter-spacing: -.01em; color: var(--ink) }
  .vbadge.vb1 { top: 16px; left: 16px; animation-delay: .25s }
  .vbadge.vb2 { top: 45%; right: -16px; animation-delay: .6s }
  .vbadge.vb3 { bottom: 18px; left: -16px; animation-delay: .95s }
  @keyframes vb-in { to { opacity: 1; transform: translateY(0) scale(1) } }
  @keyframes vbf { 0%, 100% { transform: translateY(0) rotate(0) } 50% { transform: translateY(-7px) rotate(-.6deg) } }
  @media (max-width: 560px) { .vbadge { max-width: 73% } .vb-glass { gap: 7px; padding: 6px 11px 6px 6px } .vb-ic { width: 26px; height: 26px } .vb-ic svg { width: 14px; height: 14px } .vbadge b { font-size: 11px } }

  /* OCASIÕES IDEAIS — nota de uso */
  .ba2-note { margin: 14px auto 0; max-width: 420px; text-align: center; font-size: .7rem; color: var(--muted); line-height: 1.5 }

  /* DEPOIMENTOS — grid de reviews COM foto anexada (mescla dos prints antigos) */
  .revs-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; max-width: 900px; margin: 0 auto }
  @media (max-width: 640px) { .revs-grid { grid-template-columns: 1fr; max-width: 400px } }
  .rev { background: var(--porc); border: 1px solid var(--border); border-radius: 18px; padding: 18px; box-shadow: 0 10px 30px -22px rgba(0, 0, 0, .4); display: flex; flex-direction: column; gap: 8px }
  .rev .top { display: flex; align-items: center; justify-content: space-between }
  .rev .nm { font-weight: 800; font-size: 14px; color: var(--ink) }
  .rev .vf { font-size: 11px; color: var(--ok); font-weight: 700; display: inline-flex; align-items: center; gap: 4px; background: color-mix(in oklab, var(--ok) 16%, var(--porc)); padding: 3px 8px; border-radius: 100px }
  .rev p { font-size: 13.5px; color: var(--ink); line-height: 1.55 }
  .rev .rev-pic { width: 100%; margin-top: 4px; border-radius: 12px; border: 1px solid var(--border); aspect-ratio: 1; object-fit: cover; background: var(--porc) }

  /* GARANTIA */
  .guar-wrap { position: relative; max-width: 640px; margin: 0 auto }
  .guar { position: relative; z-index: 1; max-width: 640px; margin: 0 auto; background: var(--porc); border: 1px solid var(--border); border-radius: 20px; padding: 26px; text-align: center; box-shadow: 0 14px 40px -28px rgba(0, 0, 0, .4) }
  .guar .seal { width: 66px; height: 66px; border-radius: 50%; background: var(--ac-l); display: flex; align-items: center; justify-content: center; color: var(--gold); margin: 0 auto 12px }
  .guar .seal .ic { width: 32px; height: 32px }
  .guar h3 { font-family: var(--serif); font-size: 22px; font-weight: 600; margin-bottom: 8px }
  .guar h3 span { display: inline-block; color: #fff; background: linear-gradient(135deg, var(--ac), var(--ac-d)); padding: 2px 13px; border-radius: 100px; font-family: var(--sans); font-size: 18px; font-weight: 800; vertical-align: middle }
  .guar p { color: var(--muted); font-size: 14px; line-height: 1.6 }
  .guar p b { color: var(--gold) }
  /* ANCORA DE VALOR */
  .value { background: linear-gradient(180deg, var(--porc), var(--bg)) }
  .val-emote { font-family: var(--serif); font-size: 24px; font-weight: 600; text-align: center; max-width: 24ch; margin: 0 auto 26px; line-height: 1.32; color: var(--ink) }
  .val-emote em { color: var(--gold); font-style: italic }
  .vl-sv { height: min(42svh, 300px); min-height: 260px; background: none; box-shadow: none }
  .vlb { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 18px 22px; text-align: center; opacity: var(--bv, 0); transform: translateY(calc((1 - var(--bv, 0))*24px)); pointer-events: none; transition: opacity .45s ease, transform .45s cubic-bezier(.16, 1, .3, 1); gap: 8px }
  .vl-ico { width: 62px; height: 62px; border-radius: 20px; background: var(--porc); border: 1px solid var(--border); display: grid; place-items: center; color: var(--gold); box-shadow: 0 10px 24px -12px color-mix(in oklab, var(--gold) 25%, transparent) }
  .vl-ico svg { width: 30px; height: 30px }
  .vlb b { font-family: var(--serif); font-size: 40px; font-weight: 600; color: var(--gold); line-height: 1 }
  .vl-lab { font-size: 13px; font-weight: 700; color: var(--muted); text-transform: uppercase; letter-spacing: .06em }
  .vlb p { color: var(--muted); max-width: 320px; font-size: 14px; line-height: 1.5 }
  .vl-dots { position: absolute; bottom: 12px; left: 0; right: 0; display: flex; justify-content: center; gap: 8px; z-index: 5 }
  /* Bolinha visual continua pequena (8px), mas a área clicável real é 44x44 via
     ::before invisível centralizado — abaixo disso o dedo erra o alvo no celular. */
  .vl-dots button { position: relative; width: 8px; height: 8px; border-radius: 50%; border: none; background: var(--border); cursor: pointer; transition: .2s; padding: 0 }
  .vl-dots button::before { content: ""; position: absolute; top: 50%; left: 50%; width: 44px; height: 44px; transform: translate(-50%, -50%) }
  .vl-dots button.on { background: var(--ac); width: 24px; border-radius: 100px }

  /* FAQ */
  .faq-list { max-width: 700px; margin: 0 auto; display: flex; flex-direction: column; gap: 10px }
  .faq-item { background: var(--porc); border: 1px solid var(--border); border-radius: 16px; overflow: hidden }
  .faq-item summary { list-style: none; cursor: pointer; padding: 16px 44px 16px 18px; font-size: 14.5px; font-weight: 700; color: var(--ink); position: relative; display: flex; align-items: center; min-height: 44px }
  .faq-item summary::-webkit-details-marker { display: none }
  .faq-ic { position: absolute; right: 16px; top: 50%; transform: translateY(-50%); color: var(--gold); transition: transform .25s cubic-bezier(.16,1,.3,1); flex: none }
  .faq-item[open] .faq-ic { transform: translateY(-50%) rotate(45deg) }
  .faq-body { padding: 0 18px 16px; font-size: 13.5px; color: var(--muted); line-height: 1.6 }
  .faq-item summary:focus-visible { outline: 2px solid var(--gold); outline-offset: -2px; border-radius: 12px }

  /* FOOTER */
  .ft { background: color-mix(in oklab, var(--wine) 90%, black); color: color-mix(in oklab, var(--wine) 25%, white); margin-top: 0 }
  .ft .wrap { padding: 32px 18px 0 }
  .ft-grid { display: grid; grid-template-columns: 1.4fr 1fr 1fr; gap: 30px }
  @media (max-width: 720px) { .ft-grid { grid-template-columns: 1fr; gap: 24px } }
  .ft .brand { color: #fff; font-size: 24px }
  .ft p { font-size: 13px; line-height: 1.6; margin: 10px 0 14px; max-width: 34ch }
  .ft h4 { color: #fff; font-size: 13px; font-weight: 800; text-transform: uppercase; letter-spacing: .06em; margin-bottom: 12px }
  .ft a { display: flex; align-items: center; gap: 8px; color: color-mix(in oklab, var(--wine) 25%, white); text-decoration: none; font-size: 13px; margin-bottom: 9px; transition: .15s }
  .ft a:hover { color: #fff }
  .ft-badges { display: flex; gap: 8px; flex-wrap: wrap }
  .ft-badges span { display: inline-flex; align-items: center; gap: 6px; font-size: 11px; font-weight: 700; background: rgba(255, 255, 255, .06); border: 1px solid rgba(255, 255, 255, .1); padding: 6px 11px; border-radius: 100px }
  .ft-bottom { display: flex; justify-content: space-between; gap: 12px; flex-wrap: wrap; border-top: 1px solid rgba(255, 255, 255, .1); margin-top: 32px; padding: 16px 18px 88px; font-size: 11.5px; color: color-mix(in oklab, var(--wine) 45%, white) }
  @media (min-width: 721px) { .ft-bottom { padding-bottom: 24px } }

  /* STICKY CTA */
  .stickycta { position: fixed; left: 0; right: 0; bottom: 0; z-index: 50; background: color-mix(in oklab, var(--bg) 92%, transparent); backdrop-filter: blur(10px); border-top: 1px solid var(--border); box-shadow: 0 -8px 24px -18px rgba(0, 0, 0, .5); transform: translateY(115%); transition: transform .35s cubic-bezier(.16, 1, .3, 1); will-change: transform }
  .stickycta.show { transform: translateY(0) }
  .stickycta.is-covered { visibility: hidden }
  .stickycta .wrap { display: flex; align-items: center; gap: 14px; padding: 11px 18px }
  .sk-kits { display: flex; gap: 6px; flex: 1; min-width: 0 }
  .sk-kit { flex: 1; border: 1.5px solid var(--border); background: var(--porc); border-radius: 10px; padding: 6px 4px; cursor: pointer; font-family: inherit; line-height: 1.12; transition: .15s; text-align: center }
  .sk-kit span { display: block; font-size: 10px; font-weight: 700; color: var(--muted) }
  .sk-kit b { display: block; font-size: 12.5px; color: var(--gold); font-weight: 800; margin-top: 1px }
  .sk-kit.on { border-color: var(--gold); background: var(--ac-l) }
  .stickycta .btn { white-space: nowrap; padding: 13px 18px; font-size: 14px }

  /* ── FOCO DE TECLADO ────────────────────────────────────────────────────
     Nenhuma regra no arquivo zera outline, então o navegador já mostra um
     foco padrão — mas só troca pelo roxo da marca em elementos interativos
     custom (o resto do :focus-visible nativo do browser já é suficiente). */
  .btn:focus-visible,
  .of-kit:focus-visible,
  .sk-kit:focus-visible,
  .vidcard:focus-visible,
  .ft a:focus-visible,
  .nav a:focus-visible {
    outline: 2px solid var(--gold);
    outline-offset: 3px;
  }

  /* ── REVEAL ON SCROLL — stagger por seção (via :nth-of-type) ──────────── */
  .reveal { opacity: 0; transform: translateY(22px); transition: opacity .6s cubic-bezier(.16,1,.3,1), transform .6s cubic-bezier(.16,1,.3,1) }
  .reveal.in { opacity: 1; transform: none }
  .no-js .reveal { opacity: 1; transform: none; transition: none }
  .reveal:nth-of-type(1) { transition-delay: 0ms }
  .reveal:nth-of-type(2) { transition-delay: 80ms }
  .reveal:nth-of-type(3) { transition-delay: 160ms }
  .reveal:nth-of-type(4) { transition-delay: 240ms }
  .reveal:nth-of-type(5) { transition-delay: 320ms }
  .reveal:nth-of-type(6) { transition-delay: 400ms }
  @media (prefers-reduced-motion: reduce) {
    .reveal { opacity: 1; transform: none; transition: none }
  }

  /* ═════════════════════════ v6 · polish visual ═════════════════════════ */

  /* Hero rating badge premium — score em serif, meta em small caps */
  .hero-rating { display: inline-flex; align-items: center; gap: 12px; padding: 8px 14px 8px 10px; margin-bottom: 14px; background: var(--porc); border: 1px solid var(--border); border-radius: 100px; box-shadow: 0 8px 24px -18px color-mix(in oklab, var(--gold) 30%, transparent); max-width: fit-content }
  .hero-rating .hr-score { font: 700 24px/1 var(--serif); color: var(--gold); letter-spacing: -.02em; padding: 4px 10px; background: color-mix(in oklab, var(--gold) 10%, var(--porc)); border-radius: 100px; box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--gold) 18%, transparent) }
  .hero-rating .hr-mid { display: inline-flex; flex-direction: column; gap: 3px }
  .hero-rating .hr-mid .stars { display: inline-flex; gap: 1px; color: var(--star); line-height: 1 }
  .hero-rating .hr-mid .stars .ic { width: 13px; height: 13px }
  .hero-rating .hr-meta { font-size: 11px; color: var(--muted); font-weight: 600; letter-spacing: .02em; text-transform: uppercase }
  .hero-rating .hr-meta b { color: var(--ink); font-weight: 800 }

  /* mainvid — layout 2 colunas em desktop com legenda ao lado do vídeo */
  .mainvid .mv-side { display: none }
  @media (min-width: 900px) {
    .mainvid { padding: 56px 0 64px }
    .mainvid > .wrap { max-width: 1080px }
    .mainvid .mv-grid { display: grid; grid-template-columns: minmax(0, 460px) 1fr; gap: 56px; align-items: center; text-align: left }
    .mainvid .mv-side { display: block; order: -1 }
    .mainvid .mv-side h2 { text-align: left; margin: 0 0 12px; font-size: clamp(24px, 3vw, 30px) }
    .mainvid .mv-side .lead { text-align: left; max-width: 42ch; margin: 0 }
    .mainvid .mv-quotes { margin-top: 22px; display: flex; flex-direction: column; gap: 14px }
    .mainvid .mv-quotes .mvq { display: flex; gap: 12px; align-items: flex-start; padding: 14px 16px; border-radius: 14px; background: var(--porc); box-shadow: 0 12px 32px -22px rgba(0, 0, 0, .4); border: 1px solid var(--border) }
    .mainvid .mv-quotes .mvq .qic { flex: none; width: 34px; height: 34px; border-radius: 100px; background: color-mix(in oklab, var(--gold) 14%, var(--porc)); display: grid; place-items: center; color: var(--gold) }
    .mainvid .mv-quotes .mvq p { margin: 0; font-size: 13.5px; line-height: 1.5; color: var(--ink) }
    .mainvid .mv-quotes .mvq p b { color: var(--gold) }
    .mainvid .box { margin: 0 }
  }

  /* Cards de review — elevação + hover sutil */
  .rev { transition: transform .3s cubic-bezier(.16,1,.3,1), box-shadow .3s cubic-bezier(.16,1,.3,1) }
  .rev.rev-photo { box-shadow: 0 12px 34px -22px rgba(0, 0, 0, .5), 0 2px 4px -2px rgba(0, 0, 0, .3); border-color: color-mix(in oklab, var(--gold) 12%, var(--border)) }
  @media (hover: hover) {
    .rev.rev-photo:hover { transform: translateY(-3px); box-shadow: 0 22px 44px -22px color-mix(in oklab, var(--gold) 20%, black), 0 4px 8px -4px rgba(0, 0, 0, .4) }
  }

  /* Âncora de valor — big-number statement */
  .val-hero { text-align: center; max-width: 24ch; margin: 0 auto 26px }
  .val-hero .vh-eyebrow { display: block; font-size: 11.5px; font-weight: 700; letter-spacing: .18em; text-transform: uppercase; color: var(--gold); margin-bottom: 6px }
  .val-hero .vh-num { display: block; font: 700 clamp(56px, 12vw, 88px) / 1 var(--serif); color: var(--gold); letter-spacing: -.03em; margin: 4px 0 6px }
  .val-hero .vh-num small { font: 500 .32em/1 var(--serif); color: var(--muted); letter-spacing: 0; vertical-align: 1.4em }
  .val-hero .vh-sub { display: block; font: 500 15px/1.4 var(--sans); color: var(--muted) }
  .val-hero .vh-sub b { color: var(--ink) }
  .val-hero .vh-rule { display: block; height: 1px; width: 60px; margin: 18px auto 0; background: linear-gradient(90deg, transparent, color-mix(in oklab, var(--gold) 55%, transparent), transparent) }


  /* FAQ chevron: rota 45→135deg (X) e easing mais suave */
  .faq-ic { transition: transform .32s cubic-bezier(.16,1,.3,1) }
  .faq-item[open] .faq-ic { transform: translateY(-50%) rotate(135deg) }
</style>

<!-- ============================================================
     JS — port das interações do template + hooks pro checkout Astro
     ============================================================ -->
<script>
  import { setSelectedKit } from "../scripts/cart-state";
  import { KITS, DEFAULT_KIT_INDEX, discountPercent } from "../data/kits";

  function money(v: number) { return "R$ " + v.toFixed(2).replace(".", ",") }

  // ── TOPBAR rotativa ────────────────────────────────────────────────────────
  (function () {
    const items = Array.from(document.querySelectorAll<HTMLElement>(".tb-rot .tb-item"));
    if (items.length < 2) return;
    let i = 0;
    if (!items[0].classList.contains("on")) items[0].classList.add("on");
    setInterval(() => {
      const cur = items[i], nx = (i + 1) % items.length;
      cur.classList.remove("on"); cur.classList.add("out");
      items[nx].classList.add("on");
      setTimeout(() => cur.classList.remove("out"), 600);
      i = nx;
    }, 3000);
  })();

  // ── KIT SELECTOR ──────────────────────────────────────────────────────────
  // Coordena visual do hero + cards + sticky bar + estado no localStorage
  // (via setSelectedKit) — que o CheckoutModal e criar-pix.ts leem depois.
  function pop(el: HTMLElement | null) {
    if (!el) return;
    el.classList.remove("price-pop");
    void el.offsetWidth; // força reflow pra animação reiniciar mesmo trocando 2x rápido
    el.classList.add("price-pop");
  }
  function selectKit(i: number, animate = true) {
    const k = KITS[i];
    const disc = discountPercent(k);
    document.querySelectorAll<HTMLElement>(".of-kit").forEach((el) => {
      const active = Number(el.dataset.i) === i;
      el.classList.toggle("on", active);
      el.setAttribute("aria-checked", String(active));
    });
    document.querySelectorAll<HTMLElement>(".sk-kit").forEach((el) => {
      const active = Number(el.dataset.idx) === i;
      el.classList.toggle("on", active);
      el.setAttribute("aria-checked", String(active));
    });
    const priceEl = document.getElementById("hero-price");
    const oldEl = document.getElementById("hero-old");
    const badgeEl = document.getElementById("disc-badge");
    const kitEl = document.getElementById("hero-kit");
    if (priceEl) priceEl.textContent = money(k.price);
    if (oldEl) oldEl.textContent = money(k.original);
    if (badgeEl) badgeEl.textContent = "-" + disc + "%";
    if (kitEl) kitEl.textContent = `${k.name} · R$ ${k.perUnit.toFixed(2).replace(".", ",")} por frasco`;
    if (animate) { pop(priceEl); pop(oldEl); pop(badgeEl); pop(kitEl); }
    setSelectedKit(i);
  }
  document.querySelectorAll<HTMLElement>(".of-kit").forEach((el) => {
    el.addEventListener("click", () => selectKit(Number(el.dataset.i)));
  });
  // Tilt 3D sutil no card ao passar o mouse — só desktop com mouse de verdade.
  if (window.matchMedia("(hover: hover) and (pointer: fine)").matches) {
    document.querySelectorAll<HTMLElement>(".of-kit").forEach((card) => {
      card.addEventListener("mousemove", (e) => {
        const r = card.getBoundingClientRect();
        const px = (e.clientX - r.left) / r.width - 0.5;
        const py = (e.clientY - r.top) / r.height - 0.5;
        card.style.transform = `perspective(700px) rotateX(${(-py * 4).toFixed(2)}deg) rotateY(${(px * 5).toFixed(2)}deg)`;
      });
      card.addEventListener("mouseleave", () => { card.style.transform = ""; });
    });
  }
  document.querySelectorAll<HTMLElement>(".sk-kit").forEach((el) => {
    el.addEventListener("click", () => selectKit(Number(el.dataset.idx)));
  });
  // Boot: garante que o localStorage começa com o DEFAULT_KIT_INDEX
  // pra CheckoutModal ler o valor certo antes do usuário mexer no seletor.
  // animate=false pra não disparar o pop-pulse no primeiro paint (LCP).
  selectKit(DEFAULT_KIT_INDEX, false);

  // ── 3 PASSOS ───────────────────────────────────────────────────────────────
  let psAuto: number | null = null, psIdx = 0;
  function showPasso(i: number, fromAuto = false) {
    const sec = document.getElementById("scn-passos"); if (!sec) return;
    if (!fromAuto && psAuto !== null) { window.clearInterval(psAuto); psAuto = null }
    psIdx = i;
    for (let j = 0; j < 3; j++) {
      sec.style.setProperty(`--a${j + 1}`, j === i ? "1" : "0");
      const diff = ((j - i) % 3 + 3) % 3; // 0=self, 1=próximo, 2=anterior
      const dir = diff === 1 ? 1 : diff === 2 ? -1 : 0;
      sec.style.setProperty(`--d${j + 1}`, String(dir));
    }
    sec.querySelectorAll<HTMLElement>(".ps-dot").forEach((el) => el.classList.toggle("on", Number(el.dataset.dot) === i));
    const f1 = document.getElementById("psFill1"), f2 = document.getElementById("psFill2");
    if (f1) f1.style.setProperty("--f", i >= 1 ? "1" : "0");
    if (f2) f2.style.setProperty("--f", i >= 2 ? "1" : "0");
  }
  document.querySelectorAll<HTMLElement>("#scn-passos .ps-dot").forEach((el) => {
    el.addEventListener("click", () => showPasso(Number(el.dataset.dot)));
  });
  if (document.getElementById("scn-passos")) {
    psAuto = window.setInterval(() => showPasso((psIdx + 1) % 3, true), 4200);
  }

  // ── ANCORA DE VALOR ────────────────────────────────────────────────────────
  let vlAuto: number | null = null, vlIdx = 0;
  function showValor(i: number, fromAuto = false) {
    const sec = document.getElementById("scn-valor"); if (!sec) return;
    if (!fromAuto && vlAuto !== null) { window.clearInterval(vlAuto); vlAuto = null }
    vlIdx = i;
    for (let j = 0; j < 2; j++) sec.style.setProperty(`--b${j + 1}`, j === i ? "1" : "0");
    sec.querySelectorAll<HTMLElement>(".vl-dots button").forEach((el) => el.classList.toggle("on", Number(el.dataset.vl) === i));
  }
  document.querySelectorAll<HTMLElement>("#scn-valor .vl-dots button").forEach((el) => {
    el.addEventListener("click", () => showValor(Number(el.dataset.vl)));
  });
  if (document.getElementById("scn-valor")) {
    vlAuto = window.setInterval(() => showValor((vlIdx + 1) % 2, true), 4600);
  }

  // ── VIDEO PRINCIPAL: click-to-sound ────────────────────────────────────────
  (function () {
    const v = document.getElementById("mv-video") as HTMLVideoElement | null;
    const box = document.getElementById("mv-box");
    const btn = document.getElementById("mv-play");
    if (!v || !box || !btn) return;
    btn.addEventListener("click", () => {
      v.muted = false; v.loop = false; v.controls = true;
      try { v.currentTime = 0 } catch {}
      v.play().catch(() => {});
      box.classList.add("playing");
    });
    v.addEventListener("play", () => { if (!v.muted) box.classList.add("playing") });
    v.addEventListener("pause", () => { if (!v.ended && !v.muted) box.classList.remove("playing") });
    v.addEventListener("ended", () => box.classList.remove("playing"));
  })();

  // ── VIDEOS DO MARQUEE: autoplay silencioso ────────────────────────────────
  document.querySelectorAll<HTMLVideoElement>(".vidcard video").forEach((v) => {
    v.muted = true;
    v.play().catch(() => {});
  });

  // ── REVEAL ON SCROLL ──────────────────────────────────────────────────────
  const rio = new IntersectionObserver(
    (es) => es.forEach((e) => { if (e.isIntersecting) { (e.target as HTMLElement).classList.add("in"); rio.unobserve(e.target) } }),
    { threshold: 0.05, rootMargin: "0px 0px -8% 0px" }
  );
  const revealEls = document.querySelectorAll<HTMLElement>(".reveal");
  requestAnimationFrame(() => {
    const h = window.innerHeight;
    revealEls.forEach((el) => {
      const top = el.getBoundingClientRect().top;
      if (top < h * 0.95) el.classList.add("in");
      else rio.observe(el);
    });
  });

  // ── CONTAGEM DO "R$/DIA" ao entrar na tela ─────────────────────────────────
  document.querySelectorAll<HTMLElement>("[data-countup]").forEach((el) => {
    const target = Number(el.dataset.countup);
    if (!Number.isFinite(target)) return;
    const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    let done = false;
    const cio = new IntersectionObserver(([e]) => {
      if (!e.isIntersecting || done) return;
      done = true;
      cio.disconnect();
      if (reduce) return;
      const dur = 900;
      const t0 = performance.now();
      const ease = (t: number) => 1 - Math.pow(1 - t, 3); // ease-out-cubic
      function tick(now: number) {
        const t = Math.min(1, (now - t0) / dur);
        el.textContent = (target * ease(t)).toFixed(2).replace(".", ",");
        if (t < 1) requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }, { threshold: 0.4 });
    cio.observe(el);
  });

  // ── STICKY CTA: histerese pra não flip-flopar quando .buycta encosta na dobra
  // Só ARMA depois que o usuário passou do hero. Enquanto armado, exige o
  // Comprar do hero ficar totalmente fora da tela (rootMargin negativo grande)
  // pra APARECER, e volta a ficar visível DE VERDADE pra DESAPARECER — sem
  // esse buffer o sticky ficava alternando entre show/hide a cada 1px de scroll
  // e visualmente parecia que o rodapé estava "tremendo".
  (function () {
    const sticky = document.querySelector<HTMLElement>(".stickycta");
    if (!sticky) return;
    let armed = false;
    let heroCtaVisible = true;
    function sync() { sticky!.classList.toggle("show", armed && !heroCtaVisible); }

    // Só observa o CTA do hero (o principal) — os outros .buycta ficam pra ancoragem
    // visual, mas não devem controlar aparição/desaparição do sticky.
    const heroCta = document.querySelector<HTMLElement>(".hero .buycta");
    if (heroCta) {
      const io = new IntersectionObserver(([e]) => { heroCtaVisible = e.isIntersecting; sync(); }, {
        threshold: 0,
        rootMargin: "-40px 0px -40px 0px",
      });
      io.observe(heroCta);
    }
    // Só ativa depois de rolar ~600px — evita aparecer no primeiro paint em mobile
    // quando o hero ainda pode estar parcialmente abaixo da dobra.
    function onScroll() {
      if (!armed && window.scrollY > 600) {
        armed = true;
        sync();
        window.removeEventListener("scroll", onScroll);
      }
    }
    window.addEventListener("scroll", onScroll, { passive: true });
  })();
</script>

